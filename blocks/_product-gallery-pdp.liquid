{% assign current_variant = product.selected_or_first_available_variant %}
{% assign current_color = current_variant.option1 %}
{% assign rendered_colors = '' %}

<div class="gallery-main-container" data-mobile-style="{{ block.settings.mobile_gallery_style }}">
  <div class="variant-swipers">
    {% for variant in product.variants %}
      {% assign color = variant.option1 %}

      {%- comment -%}
        Evitar renderizar el mismo color más de una vez
      {%- endcomment -%}
      {% unless rendered_colors contains color %}
        {% assign rendered_colors = rendered_colors | append: color | append: ',' %}
        {% assign color_gallery = null %}

        {%- comment -%}
          Buscar UNA galería válida para este color
        {%- endcomment -%}
        {% for v in product.variants %}
          {% if v.option1 == color and v.metafields.custom.gallery.value != blank %}
            {% assign color_gallery = v.metafields.custom.gallery.value %}
            {% break %}
          {% endif %}
        {% endfor %}

        <div
          class="variant-swiper"
          data-color="{{ color }}"
          {% unless color == current_color %}
            hidden
          {% endunless %}
        >
          {% if color_gallery %}
            <div class="variant-swiper-layout">
              <!-- Swiper -->
              <div class="swiper">
                <div class="swiper-wrapper">
                  {% for image in color_gallery %}
                    <div class="swiper-slide">
                      <img
                        src="{{ image | image_url: width: 1200 }}"
                        alt="{{ product.title }} - {{ color }}"
                        loading="lazy"
                      >
                    </div>
                  {% endfor %}
                </div>

                <div class="swiper-button-prev"></div>
                <div class="swiper-button-next"></div>
                <div class="swiper-pagination"></div>
              </div>

              <!-- Thumbnails -->
              <div class="variant-thumbs">
                {% for image in color_gallery %}
                  <button
                    class="variant-thumb"
                    data-index="{{ forloop.index0 }}"
                  >
                    <img
                      src="{{ image | image_url: width: 150 }}"
                      alt="{{ product.title }} - {{ color }}"
                      loading="lazy"
                    >
                  </button>
                {% endfor %}
              </div>
            </div>
          {% else %}
            <div class="variant-swiper-layout">
              <!-- Swiper -->
              <div class="swiper">
                <div class="swiper-wrapper">
                  {% for media in product.media %}
                    {% if media.media_type == 'image' %}
                      <div class="swiper-slide">
                        <img
                          src="{{ media | image_url: width: 1200 }}"
                          alt="{{ product.title }}"
                          loading="lazy"
                        >
                      </div>
                    {% endif %}
                  {% endfor %}
                </div>

                <div class="swiper-button-prev"></div>
                <div class="swiper-button-next"></div>
                <div class="swiper-pagination"></div>
              </div>

              <!-- Thumbnails -->
              <div class="variant-thumbs">
                {% for media in product.media %}
                  {% if media.media_type == 'image' %}
                    <button
                      class="variant-thumb"
                      data-index="{{ forloop.index0 }}"
                    >
                      <img
                        src="{{ media | image_url: width: 150 }}"
                        alt="{{ product.title }}"
                        loading="lazy"
                      >
                    </button>
                  {% endif %}
                {% endfor %}
              </div>
            </div>
          {% endif %}
        </div>
      {% endunless %}
    {% endfor %}
  </div>
</div>

<script>
  document.addEventListener('variant:update', (event) => {
    if (!event.detail || !event.detail.resource) return;

    const variant = event.detail.resource;
    const variantId = String(event.detail.resource.id);
    document.querySelectorAll('.variant-gallery').forEach((gallery) => {
      gallery.hidden = gallery.dataset.variantId !== variantId;
    });

    // Color seleccionado (option1)
    const selectedColor = variant.option1;

    // Actualizar texto de color
    const colorEl = document.querySelector('[data-selected-color]');
    if (colorEl) {
      colorEl.textContent = selectedColor || '—';
    }

    // (Opcional) otros textos
    const skuEl = document.querySelector('[data-selected-sku]');
    if (skuEl) {
      skuEl.textContent = variant.sku || 'Sin SKU';
    }
  });
</script>

<script>
  let activeSwiper = null;
  let lastColor = null;
  const slideIndexByColor = {};

  function initSwiper(container, color, startIndex = 0) {
    const swiperEl = container.querySelector('.swiper');
    const thumbs = container.querySelectorAll('.variant-thumb');

    if (!swiperEl) return;

    // 🔥 LIMPIEZA TOTAL (clave)
    thumbs.forEach((t) => t.classList.remove('is-active'));

    const paginationEl = swiperEl.querySelector('.swiper-pagination');

    function updateIndicator(swiper) {
      if (!paginationEl) return;
      let indicator = paginationEl.querySelector('.swiper-pagination-indicator');
      if (!indicator && swiper.pagination.bullets.length > 0) {
        indicator = document.createElement('div');
        indicator.className = 'swiper-pagination-indicator';
        paginationEl.appendChild(indicator);
      }

      if (indicator && swiper.pagination.bullets.length > 1) {
        const bullets = swiper.pagination.bullets;
        const firstBullet = bullets[0];
        const lastBullet = bullets[bullets.length - 1];
        const totalDistance = lastBullet.offsetLeft - firstBullet.offsetLeft;

        // Swiper progress goes from 0 to 1
        const currentPos = firstBullet.offsetLeft + swiper.progress * totalDistance;

        // Liquid Stretch Effect Logic
        const bulletCount = bullets.length - 1;
        const localProgress = (swiper.progress * bulletCount) % 1;
        const stretchAmount = Math.sin(localProgress * Math.PI) * 0.8;
        const scaleX = 1 + stretchAmount;

        // Apply transform: translateX + scaleX
        indicator.style.transform = `translateX(${currentPos}px) scaleX(${scaleX})`;
        indicator.style.transformOrigin = localProgress > 0.5 ? 'left center' : 'right center';
      } else if (indicator && swiper.pagination.bullets[0]) {
        indicator.style.transform = `translateX(${swiper.pagination.bullets[0].offsetLeft}px) scaleX(1)`;
      }
    }

    activeSwiper = new Swiper(swiperEl, {
      slidesPerView: 1,
      spaceBetween: 0,
      loop: false,
      speed: 600,
      initialSlide: startIndex,
      on: {
        init(swiper) {
          setTimeout(() => updateIndicator(swiper), 100); // Wait for bullets to render
        },
        progress(swiper) {
          updateIndicator(swiper);
        },
        setTranslate(swiper) {
          updateIndicator(swiper);
        },
        slideChange(swiper) {
          slideIndexByColor[color] = swiper.activeIndex;
          thumbs.forEach((t) => t.classList.remove('is-active'));
          const active = thumbs[swiper.activeIndex];
          if (active) active.classList.add('is-active');
        },
      },
      pagination: {
        el: paginationEl,
        clickable: true,
        dynamicBullets: false,
      },
      effect: 'fade',
      fadeEffect: {
        crossFade: true,
      },
    });

    // Click thumbs
    thumbs.forEach((thumb) => {
      thumb.addEventListener('click', () => {
        const index = Number(thumb.dataset.index);
        activeSwiper.slideTo(index);
      });
    });

    // Estado inicial
    const initialThumb = thumbs[startIndex];
    if (initialThumb) initialThumb.classList.add('is-active');
  }

  function destroySwiper() {
    if (activeSwiper) {
      activeSwiper.destroy(true, true);
      activeSwiper = null;
    }
  }

  function showColorSwiper(color) {
    document.querySelectorAll('.variant-swiper').forEach((block) => {
      const isActive = block.dataset.color === color;
      block.hidden = !isActive;

      if (isActive) {
        const startIndex = slideIndexByColor[color] !== undefined ? slideIndexByColor[color] : 0;

        destroySwiper();
        initSwiper(block, color, startIndex);

        lastColor = color;
      }
    });
  }

  document.addEventListener('variant:update', (event) => {
    const variant = event.detail?.resource;
    if (!variant) return;

    showColorSwiper(variant.option1);
  });

  document.addEventListener('DOMContentLoaded', () => {
    const initial = document.querySelector('.variant-swiper:not([hidden])');
    if (!initial) return;

    const color = initial.dataset.color;
    slideIndexByColor[color] = 0;

    initSwiper(initial, color, 0);
    lastColor = color;
  });
</script>

<style>
  .gallery-main-container {
    width: 100%;
  }

  .swiper {
    width: 100%;
    height: 100%;
  }

  /* Base Styles: Mobile */
  .variant-swiper-layout {
    display: grid;
    grid-template-columns: 1fr;
    gap: 16px;
    align-items: flex-start;
  }

  .variant-thumbs {
    display: none;
    flex-direction: row;
    gap: 8px;
    overflow-x: auto;
    order: 2;
  }

  .gallery-main-container[data-mobile-style='thumbs'] .variant-thumbs {
    display: flex;
  }

  .variant-thumb {
    padding: 0;
    border: 1px solid transparent;
    background: none;
    cursor: pointer;
    flex-shrink: 0;
    width: 80px;
  }

  .variant-thumb img {
    width: 100%;
    display: block;
  }

  .variant-thumb.is-active {
    border-color: #000;
  }

  .swiper-pagination {
    position: absolute;
    bottom: 16px !important;
    left: 16px !important;
    transform: none !important;
    z-index: 10;
    display: none;
    width: auto !important;
    text-align: left;
    white-space: nowrap;
  }

  .gallery-main-container[data-mobile-style='dots'] .swiper-pagination {
    display: block;
  }

  .swiper-pagination-bullet {
    background: #fff !important;
    opacity: 0.5;
    width: 8px !important;
    height: 8px !important;
    margin: 0 4px !important;
    border-radius: 50% !important;
    transition: opacity 0.3s;
  }

  .swiper-pagination-bullet-active {
    opacity: 0.5 !important;
  }

  .swiper-pagination-indicator {
    position: absolute;
    top: 50%;
    left: 0;
    width: 8px;
    height: 8px;
    background: #fff;
    border-radius: 50%;
    z-index: 11;
    pointer-events: none;
    margin-top: -4px; /* Center vertically */
    transition: transform 0.05s linear, opacity 0.3s; /* Even faster for liquid feel */
    will-change: transform;
  }

  /* Desktop Styles */
  @media (min-width: 769px) {
    .variant-swiper-layout {
      grid-template-columns: 1fr 80px;
    }

    .variant-thumbs {
      display: flex;
      flex-direction: column;
      order: unset;
      overflow-x: visible;
    }

    .variant-thumb {
      width: 100%;
    }

    .swiper-pagination {
      display: none !important;
    }
  }
</style>

{% schema %}
{
  "name": "Galería de Variantes",
  "tag": "section",
  "class": "section-variant-gallery",
  "settings": [
    {
      "type": "header",
      "content": "Configuración de la Galería"
    },
    {
      "type": "select",
      "id": "mobile_gallery_style",
      "label": "Estilo de galería en movil",
      "options": [
        {
          "value": "thumbs",
          "label": "Miniaturas (Thumbs)"
        },
        {
          "value": "dots",
          "label": "Puntos (Dots)"
        }
      ],
      "default": "thumbs"
    }
  ],
  "presets": [
    {
      "name": "Galería de Variantes Personalizada"
    }
  ]
}
{% endschema %}
